import { StyleSheet, Text, View } from "react-native";
import React from "react";

import ScreenTemplate from "../template/ScreenTemplate";

const AvailableTrucks = () => {
  /**
   * TODO:
   * 1. in the dataase, define a schema and create documents for trucks
   * 2. query and search the database for data that relates to the information provided by the user
   * 3. display the information of the trucks based on the information provided by the user
   */

  return (
    <ScreenTemplate>
      <Text>AvailableTrucks</Text>
    </ScreenTemplate>
  );
};

export default AvailableTrucks;

const styles = StyleSheet.create({});
